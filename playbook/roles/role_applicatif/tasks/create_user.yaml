---
- name: "add group {{ app_group }}"
  group: "{{ app_group }}"
  gid: "{{ app_gid }}"
  state: present

- name: create user
  user:
    name: "{{ app_user }}"
    groups: "{{sys_group}},{{ app_group }}"
    append: yes
    uid: "{{ app_uid }}"
