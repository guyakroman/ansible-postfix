---
- name: create directory
  file:
    path: "{{ item }}"
    state: directory
    group: "{{ sys_group }}"
    mode: "0774"
    recurse: yes
  with_items:
    - "{{ app_repo }}"
    - "{{ app_data }}"
    - "{{ app_var }}"
