---
- name: deploy kafla cluster
  hosts: kafka
  become: true
  vars:
    app_name: kafka
    app_user: "{{ app_name }}"
    app_group: kafka
    app_gid: 5001
    app_uid: 5001
  roles:
    - { role: role_system, state: install }
    - { role: role_applicatif }
